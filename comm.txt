  # brand = sorted(car['Brand'].unique())
    # model = sorted(car['Model'].unique())
    # casemat = sorted(car['Case Material'].unique())
    # strap = car['Strap Material'].unique()
    # movement = sorted(car['Movement Type'].unique())
    # waterrezis = sorted(car['Water Resistance'].unique())
    # casediam = sorted(car['Case Diameter (mm)'].unique())
    # casethick = car['Case Thickness (mm)'].unique()
    # bandwid = sorted(car['Band Width (mm)'].unique())
    # dialcol = sorted(car['Dial Color'].unique())
    # crystalmat = sorted(car['Crystal Material'].unique())
    # complicattion = car['Complications'].unique()
    # powerres = car['Power Reserve'].unique()